This repository contains an example of how to implement soft delete functionality in a Spring Boot application,
using Spring Data JPA for its ORM. The default Spring Data JPA implementation (Hibernate) has been used.

The project is based on:
- Spring Boot 3.0.3
- Kotlin 1.7.22